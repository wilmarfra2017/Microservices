apiVersion: v1
kind: Pod
metadata:
  name: gateway-pod
  namespace: production
  labels:
    app: gateway
    type: microservice
spec:
  containers:
  - name: gateway-app
    image: mart800749/serviciosapigateway:latest
    ports:
    - containerPort: 80