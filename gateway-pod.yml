apiVersion: v1
kind: Pod
metadata:
  name: gateway-pod
  namespace: production
  labels:
    app: gateway
    type: microservice
spec:
  containers:
  - name: gateway-app
    image: mart800749/microservicesdevops:gateway-$(Build.BuildId)
    ports:
    - containerPort: 80